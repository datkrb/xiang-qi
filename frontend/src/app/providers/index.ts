// Global context providers
// Temporarily re-exported from legacy context folder during migration

export { GameProvider, useGame } from "./GameProvider";
export { SettingsProvider, useSettings } from "./SettingsProvider";
